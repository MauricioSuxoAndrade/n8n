version: '3.7'

services:
  n8n_web:
    image: docker.n8n.io/n8nio/n8n
    container_name: n8n_web
    restart: always
    environment:
      - N8N_USER_MANAGEMENT_ENABLED=true
      - N8N_RUNNERS_ENABLED=true
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
      - N8N_HOST=warmisalud.duckdns.org
      - N8N_PORT=5678
      - N8N_PROTOCOL=https
      - N8N_EDITOR_BASE_URL=https://warmisalud.duckdns.org
      - WEBHOOK_URL=https://warmisalud.duckdns.org
    expose:
      - "5678"                # solo visible para caddy en la misma red
    volumes:
      - n8n_web_data:/home/node/.n8n
    networks: [webnet]

  caddy:
    image: caddy:2-alpine
    container_name: n8n_caddy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - n8n_web
    networks: [webnet]

networks:
  webnet:

volumes:
  n8n_web_data:
  caddy-data:
  caddy-config:
